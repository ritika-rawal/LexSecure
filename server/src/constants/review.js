export const REVIEW_RATING_LIMITS = Object.freeze({
  MINIMUM: 1,
  MAXIMUM: 5,
});

export const REVIEW_COMMENT_LIMITS = Object.freeze({
  MINIMUM: 10,
  MAXIMUM: 1000,
});

export const REVIEW_LIST_LIMITS = Object.freeze({
  DEFAULT: 5,
  MAXIMUM: 20,
});
