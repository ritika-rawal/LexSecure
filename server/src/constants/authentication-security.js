export const LOGIN_SECURITY_LIMITS = Object.freeze({
  MAXIMUM_FAILED_ATTEMPTS: 5,
  LOCKOUT_DURATION_MS: 15 * 60 * 1000,
  CAPTCHA_AFTER_SESSION_FAILURES: 3,
  CAPTCHA_REQUIREMENT_DURATION_MS: 15 * 60 * 1000,
});

export const CAPTCHA_LIMITS = Object.freeze({
  TOKEN_MAXIMUM_LENGTH: 2048,
  VERIFICATION_TIMEOUT_MS: 5_000,
});

export const CAPTCHA_ERROR_CODES = Object.freeze({
  REQUIRED: 'CAPTCHA_REQUIRED',
  INVALID: 'CAPTCHA_INVALID',
  UNAVAILABLE: 'CAPTCHA_UNAVAILABLE',
});

export const RATE_LIMIT_WINDOWS_MS = Object.freeze({
  API: 15 * 60 * 1000,
  AUTHENTICATION: 15 * 60 * 1000,
  REGISTRATION: 60 * 60 * 1000,
  PASSWORD_RESET: 60 * 60 * 1000,
});

export const RATE_LIMIT_MAXIMUMS = Object.freeze({
  API: 300,
  CSRF_TOKEN: 60,
  FAILED_LOGIN: 10,
  FAILED_MFA_VERIFICATION: 10,
  REGISTRATION: 5,
  PASSWORD_RESET_REQUEST: 3,
  PASSWORD_RESET_CONFIRM: 10,
  PASSWORD_CHANGE: 5,
});

export const RATE_LIMIT_ERROR_CODE = 'RATE_LIMIT_EXCEEDED';
